<div class="container">
  <div class="row">
    <div class="col-12">
      {% include 'breadcrumbs' %}
      <div class="title-box">
          <h1>{{ page.title }}</h1>
      </div>
      <div class="content-box">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>